"""limit-up (LU) core library.

All business logic lives here so it can be exposed two ways:
- apps/api  (FastAPI, for the local web app)
- apps/mcp  (MCP server, for Claude Desktop/Code and the headless `claude -p` brain)
"""

__version__ = "0.0.0"
